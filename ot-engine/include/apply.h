#pragma once
#include "operation.h"
#include <set>
#include <string>
#include <vector>

std::string applyTransformations(std::string currentDocument,
                                 Operation latestOperation);
