apiVersion: v1
kind: Service
metadata:
  name: docmanager-service
spec:
  selector:
    app: docmanager-service
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
    - name: grpc
      protocol: TCP
      port: 9090
      targetPort: 9090
  type: ClusterIP
