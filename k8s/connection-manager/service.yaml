apiVersion: v1
kind: Service
metadata:
  name: connection-manager-service
spec:
  selector:
    app: connection-manager-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
