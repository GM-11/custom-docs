apiVersion: v1
kind: Service
metadata:
  name: connection-manager-service
spec:
  type: NodePort
  selector:
    app: connection-manager-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
